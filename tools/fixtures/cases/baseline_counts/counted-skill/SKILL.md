---
name: counted-skill
description: A fixture skill whose body repeats two planted defect types many times, so the baseline count map holds keys with counts well above one.
---

# Fixture

The build runs first — the report comes after.
The tests run next — the log arrives after.
The deploy runs third — the alert arrives after.
The rollback runs last — the page arrives after.

The audit runs first; the summary comes after.
The backup runs next; the restore comes after.
