---
name: sk008-skill
description: A fixture skill that names one sibling that exists and one sibling that does not exist.
---

# Fixture

Load the `ghost-sibling` skill first.
Then load the `real-sibling` skill.
